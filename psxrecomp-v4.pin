# Pinned commit of github.com/mstan/psxrecomp (the shared framework) used to
# build this repo. Bumping this is a deliberate, reviewable change — psxrecomp
# is a separate repo evolving on its own cadence. Update via:
#   git -C ../psxrecomp rev-parse master > /tmp/sha && \
#     sed -i "s|^sha=.*|sha=$(cat /tmp/sha)|" psxrecomp-v4.pin
branch=master
sha=34561b541887d84dd43023b12fb52c14b1868b4a
# Bumped 2026-06-11 for the v0.1.7-alpha release: re-pointed to psxrecomp master
# (34561b5). master is a strict superset of the old feat/overlay-jit-cache pin
# (7249123) — it contains the entire overlay-jit-cache campaign (steps 2.7/2.8,
# Bug A fix, DISPATCH_INTERIOR) PLUS the Bug D dispatch call-contract (2439d4d),
# DMA hardening, the SSAA/AA + optional-bilinear video commits, and the Bug B
# freeze fix. feat/overlay-jit-cache was fully merged into master and deleted.
# NOTE: 2439d4d is an emitter change, so the overlay ABI bumped to v2; rebuild
# the overlay cache under this framework (the v2 loader rejects/deletes v1 DLLs).
# ---- history ----
# 2026-06-06 (d22c9ce / 7249123): mmx6 framework + diagnostic tooling.
# 2026-06-07: re-pointed branch -> feat/overlay-jit-cache (same commit then).
